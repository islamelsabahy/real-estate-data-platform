# Golden Record Strategy

Where multiple systems describe the same entity:

1. define trusted source per attribute,
2. normalize identifiers,
3. apply survivorship rules,
4. record source lineage,
5. retain aliases.

Do not merge records solely on fuzzy name matching in production.
